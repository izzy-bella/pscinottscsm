# Backend Notes

This version adds a proper attendance management foundation:

- `AttendanceSession` model
- `AttendanceRecord` model with status values
- attendance session routes and controller
- dashboard attendance summary
- demo attendance seed inside the member import script

Next likely backend additions:

- member document uploads
- member profile image storage
- giving CRUD endpoints
- care case CRUD endpoints
