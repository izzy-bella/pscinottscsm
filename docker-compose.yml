services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: church_cms
    ports:
      - "5432:5432"
    volumes:
      - church_cms_pgdata:/var/lib/postgresql/data

volumes:
  church_cms_pgdata:
